# URL the browser uses to reach the API.
# In production on Railway, use a relative path so it works without knowing your domain:
NEXT_PUBLIC_API_URL=/api

# During local development, point to your running backend:
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
